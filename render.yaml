services:
  - type: web
    name: truparts-backend
    runtime: node
    plan: free
    buildCommand: cd backend && rm -rf node_modules package-lock.json && npm install && npm list @nestjs/config && npm run build
    startCommand: cd backend && npm run start:prod
    envVars:
      - key: DATABASE_URL
        sync: false
      - key: WHATSAPP_API_TOKEN
        sync: false
    autoDeploy: false
